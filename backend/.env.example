# API authentication keys
# Provide either a single API_KEY or a comma-separated list in API_KEYS
# Example (single):
# API_KEY=dev-local-key
# Example (multiple):
# API_KEYS=key1,key2,key3

# Server
PORT=3001

# Background processing
# Set to 'true' to force-disable worker threads (useful in constrained or debug environments).
# Workers are automatically disabled during tests.
# DISABLE_WORKER=true
